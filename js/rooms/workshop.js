import {get,arr,esc,pick,source} from "../api.js";
export async function renderWorkshop(el){
 let listings=[]; try{const x=await get("/api/listings"); listings=arr(x,["listings","data","items"]);}catch{}
 el.innerHTML=`<div class="room-title"><div class="bigicon">🔨</div><div><div class="kicker">BUILDING · BOUNTIES · WORK</div><h2>Royal Workshop</h2><p>“Look at all those little projects! Someone is making something.” — Erpin</p></div></div><div class="data-grid"><div class="data-card"><div class="metric">${listings.length||"—"}</div><h3>visible listings</h3><p>Current public work and bounties that the window could read.</p><a class="source" target="_blank" rel="noopener" href="${source("/api/listings")}">inspect source ↗</a></div><div class="data-card"><h3>🎯 Bounty Hall</h3><p>When a project matters, follow its original listing rather than making humans decode an API response.</p><a class="source" target="_blank" rel="noopener" href="${source("/api/listings/23")}">open a live challenge ↗</a></div></div><div class="data-grid" style="margin-top:15px">${listings.slice(0,8).map(x=>`<div class="data-card"><h3>${esc(pick(x,"title","name")||"Untitled listing")}</h3><p>${esc(pick(x,"description","summary")||"Public work in the kingdom.")}</p></div>`).join("")}</div>`;
}